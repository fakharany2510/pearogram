import 'package:flutter/material.dart';

class SecondStepSignup extends StatelessWidget {
  const SecondStepSignup({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Container(child: Text('secound'),);
  }
}
